# Carver

Carver is a custom memory allocator written in C++ as a learning project. The goal is to build an allocator from scratch while exploring how dynamic memory allocation works under the hood.

## Current Features

- Simple bump allocator
- Placement `new` support
